#include <stdio:h>
#include < limits. h>
void main()
{
   inta [100] ,i,n,l1,l2;
   print f("emter the size ") ;
   scanf(%d", & n) ;
   Print f(\nenter the elements ") ;
   for (i=0;i<n:i++)
{
   Scanf (" %d", & a[i] ;
} 
